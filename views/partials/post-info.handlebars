
   
      <div class="post">
        
        <h3>{{title}}</h3>

        <div class="bottom_post">
        by {{user.username}} on
        {{format_date created_at}}
        <h5>content: {{post-text}}</h5>
        <a href="/post/{{id}}" class="comment_link">{{comments.length}} {{format_plural "comment" comments.length}}</a>
        </div>

      </div>
    